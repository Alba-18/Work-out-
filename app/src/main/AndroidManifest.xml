<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.work_out_">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        android:maxSdkVersion="31" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Workout">

        <receiver
            android:name=".Notifications"
            android:enabled="true"
            android:exported="false"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="com.google.android.gcm.demo.app" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCqbo86VByiGB8AKpuMo5MK9b6ZHULj_vs" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfileActivity" /> <!-- <activity android:name=".ConfigActivity" />¡ -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".FinishedExerciseActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PushupsStartActivity" />
        <activity android:name=".RunningStartActivity" />
        <activity android:name=".SitupsStartActivity" />
        <activity android:name=".WalkingStartActivity" />
        <activity android:name=".ActivitiesActivity" />

        <service
            android:name=".NotificationService"
            android:exported="false"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="app.tutorialspoint.com.notifyme.NotificationService" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service> <!-- <activity android:name=".RegisterActivity"></activity> -->
        <activity
            android:name=".ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>